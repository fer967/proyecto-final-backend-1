<h1>Mis Productos</h1>
<div class="container">
    {{#each productos}}
        <div class="card">
            <p>{{this.title}}</p>
            <p>{{this.description}}</p>
            <p>{{this.price}}</p>
            <p>{{this.code}}</p>
            <p>{{this.stock}}</p>
            <button class="button">agregar al carrito</button>
        </div>
    {{/each}}
</div>  
